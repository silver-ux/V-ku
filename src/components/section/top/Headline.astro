---
import Button from "@components/Button.astro";

const { style } = Astro.props;
---

<div class="headline" style={style}>
    <div class="top-text">
        <p>
            <span class="marker">VTuberやクリエイターの</span>
        </p>
        <p>
            <span class="marker">活動を支援するバーチャル区役所</span>
        </p>
    </div>
    <p class="desc">
        「案件紹介」だけでなく「制作サポート」や「福祉サポート」など、みなさんの活動に更に寄り添ったサポートができるのがV区の強みです。
    </p>
    <div class="btn-place">
        <Button style="font-size:clamp(18px, 2vw, 24px)">Discordへ</Button>
    </div>
</div>

<style>
    .headline {
        color: white;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        place-content: center;
        padding-top: 150px;
        .top-text {
            p {
                font-size: clamp(28px, 5vw, 50px);
                width: 100%;
                font-weight: bold;
            }
        }
    }
    .marker {
        background: linear-gradient(transparent 60%, #f2cf39 0%);
        display: inline;
        padding: 0 5px 0;
        background-repeat: no-repeat;
        /* background-size: 0% 100%;
        animation: marker 2s ease forwards 1.5s; */
    }
    .btn-place {
        display: flex;
        justify-content: end;
        padding-top: 60px;
    }
    .desc {
        width: min(650px, 80%);
        margin-left: auto;
        margin-top: clamp(3%, 10%, 30px);
        font-size: clamp(14px, 2vw, 20px);
        background-color: rgba(0, 0, 0, 0.5);
        padding: 1em 2em;
        border-radius: 10px;
    }

    @media screen and (max-width: 650px) {
        .btn-place {
            justify-content: center;
        }
    }
</style>
