---
import Button from "@components/Button.astro";

const fontSize = "clamp(18px, 2vw, 24px)";
---

<div class="headline">
    <div class="top-text">
        <h1>
            <span class="marker"
                >VTuberやクリエイターの<br
                />活動を支援するバーチャル区役所</span
            >
        </h1>
    </div>
    <p class="desc">
        「案件紹介」だけでなく「制作サポート」や「福祉サポート」など、みなさんの活動に更に寄り添ったサポートができるのがV区の強みです。
    </p>
    <div class="btn-place">
        <Button fontSize={fontSize}>Discordへ</Button>
    </div>
</div>

<style>
    .headline {
        color: white;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        place-content: center;
        padding-top: 150px;
        .top-text {
            h1 {
                /* font-size: clamp(28px, 5vw, 50px); */
                /* 1200 ~ 650 */
                font-size: clamp(1.75rem, 0.833rem + 3.67vw, 3rem);
                line-height: 1.5;
                width: 100%;
            }
        }
    }
    .marker {
        background: linear-gradient(transparent 60%, #f2cf39 0%);
        display: inline;
        padding: 0 5px 0;
        background-repeat: no-repeat;
        /* background-size: 0% 100%;
        animation: marker 2s ease forwards 1.5s; */
    }
    .btn-place {
        display: flex;
        justify-content: end;
        padding-top: 60px;
    }
    .desc {
        width: min(650px, 80%);
        margin-left: auto;
        /* margin-top: clamp(3%, 10%, 30px); */
        margin-top: min(30px, 20%);
        /* font-size: clamp(14px, 2vw, 20px); */
        font-size: clamp(0.875rem, 0.625rem + 1vw, 1.25rem);
        background-color: rgba(0, 0, 0, 0.5);
        padding: 1em 2em;
        border-radius: 10px;
    }

    @media screen and (max-width: 650px) {
        .btn-place {
            justify-content: center;
        }
        .desc {
            text-align: justify;
        }
    }
</style>
