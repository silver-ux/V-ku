---
const { heading, text, smallText } = Astro.props;
---

<div class="text-wrap">
    <h3>{heading}</h3>
    <p class="text-content">
        {text}
    </p>
    <p class="bottom">
        <small>{smallText}</small>
    </p>
</div>

<style>
    .text-wrap {
        width: 90%;
        text-align: center;
        margin-top: 1.5rem;

        h3 {
            padding-bottom: 10px;
            /* 28 ~ 30px */
            font-size: clamp(1.75rem, 1.698rem + 0.22vw, 1.875rem);
            padding-left: 1em;
        }
        .text-content {
            /* 14 ~ 16px */
            font-size: clamp(0.875rem, 0.823rem + 0.22vw, 1rem);
            text-align: center;
        }
    }
    .bottom {
        text-align: center;
        margin-top: 10px;
        /* 12 ~ 16px */
        font-size: clamp(0.75rem, 0.646rem + 0.44vw, 1rem);
    }

    @media screen and (min-width: 768px) {
    }
    @media screen and (min-width: 1280px) {
        .text-wrap {
            h3 {
                /* font-size: 32px; */
            }
            .text-content {
                /* font-size: 20px; */
            }
        }
        .bottom {
            margin-top: 10px;
            /* font-size: 16px; */
        }
    }
</style>
